"""Release and operational verification helpers.

These modules are intentionally separate from the runtime application. They may inspect or
copy release artifacts and databases, but they never alter the source database.
"""
